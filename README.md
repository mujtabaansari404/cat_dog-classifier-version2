# 🐱🐶 Cat vs Dog Classifier – Streamlit App

A simple and interactive Streamlit web application that classifies uploaded images as either a cat or a dog using a pre-trained Convolutional Neural Network (CNN) model (`model/cat_dog_model.h5`). The app supports image input via local file upload or by providing an image URL.

## ⚡ Features

- Upload images directly from your computer or enter an image URL.
- Displays the predicted class (Cat or Dog) along with the model's confidence score.
- Clean and user-friendly Streamlit interface.
- Professional footer with a link to the GitHub repository.

## install
pip install -r requirement.txt

